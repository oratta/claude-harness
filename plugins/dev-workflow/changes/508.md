# W が CI の検査を return 前に実行する（#508）

- `skills/develop/references/roles/worker.md`: (3a) の全経路共通原則で、PR・push の workflow から検査コマンドを収集して実行する。環境セットアップとの区別、対象外の運用系 workflow、workflow 不在時のフォールバック、ツール未導入時の扱いと守備範囲を明記する。コード直行の手順 5 は共通原則を参照し、return には収集したコマンドと各 exit code を求める
- `tests/develop-worker-ci-checks.bats`: 共通原則・守備範囲・return 契約の文面を検査する
- `tests/develop-roles.bats`: 既存の (3a) return 契約テストを「検査コマンド」の文言に合わせる
- `openspec/changes/worker-return-ci-checks/`: 仕様レビューの SHOULD_FIX 2 件を反映し、実装・検証済みの tasks を更新する
