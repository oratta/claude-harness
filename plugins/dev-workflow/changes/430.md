# README 2 か所のエピックの回し方の記述を Orca 経路込みにそろえる（#430）

`README.md:28` と `plugins/dev-workflow/README.md:15` は「子 issue ごとに 1 ループを `isolation: "worktree"` で並列に回す」とだけ書いており、PR #428 で足した Orca 経路（並列可能な子が 2 件以上・`orca` が PATH にあり・本体が Orca 管理のワークツリーにいれば Orca の子ワークツリーの独立セッション、それ以外はサブエージェント方式）が反映されていなかった。README だけを読んだ人が、エピックの本体が子ごとに W を起こすものだと思ったまま `orca` 環境で `/develop <エピック>` を始め、別セッションが立ち上がる動きを予期しないという指摘（PR #428 レビューの F2）への追随修正。

- 2 行とも「子 issue ごとに 1 ループを並列に回す（並列可能な子が 2 件以上で `orca` があり本体が Orca 管理のワークツリーにいれば Orca の子ワークツリーの独立セッション、それ以外は `isolation: "worktree"` のサブエージェント。`scripts/epic-dispatch.sh`）」に置き換えた（正本は `plugins/dev-workflow/skills/develop/SKILL.md`「回し方」）
- `plugins/dev-workflow/README.md:15` は末尾の「子が全部マージされただけでは閉じない」をそのまま残した
- 振る舞いを変えない文言修正のため版は上げていない
