# プロンプトから経緯の記述を外す（#506）

`/claude-api prompt-audit` の監査で見つかった、由来だけを示す issue 番号・日付を今の運用の説明に直した。

- `skills/capability-registry/1password.md:14`: 「（2026-07-30 主判断。実績: #49 / #58、moko の `moko--TRELLO_TOKEN`）」を「（主が決めた運用。実例: moko の `moko--TRELLO_TOKEN`）」に書き換え。エージェントが rw SA 経由の CLI で自分で登録するという運用自体は変えていない
- `skills/capability-registry/SKILL.md:38` の「suimei のマーケ日報 PR #235」は実装例への手がかりとして残す判断済み（触っていない）
