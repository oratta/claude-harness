# wt-clean の指示書から経緯の記述を外す（#506）

`/claude-api prompt-audit` の監査で見つかった、旧版との差分として書かれた文を今のルールとして書き直した。

- `skills/wt-clean/SKILL.md:345`: 対象選択で確認を挟まない理由を、過去の仕様への言及ではなく「対象選択で質問すると、そこで処理が止まる／途中で終わる」という理由だけの文に書き換え
- `skills/wt-clean/SKILL.md:527`: 判定規則の説明から過去の判定基準への言及を削除し、「判定規則。」だけを残す（直後の表が上から順の最初の一致で判定を決めるので、判定自体は変わらない）
- `skills/wt-clean/SKILL.md` の 905〜1311 行を監査した。旧版との差分表現・由来だけの issue 番号・日付と件数だけで理由を示す文は 1 箇所あった。`:994` の状況表示の出力例が、稼働シグナルによる保留を「旧判定なら」という形で書いていたため、今の判定結果だけを示す文（稼働シグナルがあるため自動処理を保留）に書き換えた。この範囲に出てくる `PR #289` や `2026-08-07 14:32` などの番号・日付はすべて完了レポート・状況表示の出力例に含まれるサンプル値であり、ルールの由来を示す記述ではないため対象外とした
