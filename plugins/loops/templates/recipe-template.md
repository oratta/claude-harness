# レシピ: <name>

> このファイルは `plugins/loops/templates/recipe-template.md` の雛形。`recipes/<name>.md` にコピーして使う。
> 規約は `plugins/loops/references/recipe-format.md` を参照。7 見出しはすべて必須。

## ループ型

<ターンベース / ゴールベース / タイムベース / プロアクティブ のいずれか 1 つ>
（選定基準は `references/loop-types.md` の「何を手放すか」を参照）

## 目的

<何を達成するか / なぜループにするか を 1〜3 行で>

## 起動コマンド

<ネイティブプリミティブのコピペ可能な文字列をそのまま書く。例:>

```
/goal <定量ゴール>, stop after <N> tries
```

- 起動コマンドは **/goal・/loop・/schedule・skill 起動** のいずれかで書く。
- **独自 CLI やラッパースクリプトを作らない**。ループを回す driver は書かず、反復・停止はネイティブに委ねる。

## 停止基準

<最大試行数 / 時間 / 定量ゴール のいずれか 1 つ以上を必ず書く（必須）>
（複数の独立した出口: ゴール達成 / 予算切れ / 反復上限 / 無進捗検出 を書けるとより堅牢）

## 前提

<依存する skill・設定・データ・環境・参照パス>
<実行環境の制約があればここに（例: ローカルの ~/.claude/projects/ jsonl を読むためローカル実行必須）>

## コスト注意

<公式トークン管理 6 項目のうち該当するもの（頻度最小化 / 決定論部分のスクリプト化 / パイロット実行 等）>

## エスカレーション

<人間へ引き渡す条件。merge / close / force 系の不可逆操作は非破壊デフォルト（Draft PR まで）に倒しここでエスカレーション>
