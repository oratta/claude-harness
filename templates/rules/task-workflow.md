# Task Execution Rules

**適用対象: 対話型セッションのみ。`longrun:exec` / `openspec:apply` の自律実行中は適用しない。**

Claude のデフォルト（スコープ超過禁止・UI テスト後に報告）に加えて、以下を守る。

## タスク境界制御

- ユーザーが動作確認可能な単位でタスクを分割する
- 動作確認可能な単位が完了したら**作業を止める**
- ユーザーに確認を促す → 「お手元で動作確認をお願いできますか？」
- **ユーザーの動作確認を待たずに次のタスクに進まない**
